5a6,7
> auth       optional   pam_fscrypt.so
> auth       optional   pam_gnupg.so        store-only
17a20
> session    optional   pam_fscrypt.so       drop_caches lock_policies
19a23
> session    optional   pam_gnupg.so         no-autostart
