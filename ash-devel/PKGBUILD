# Maintainer: Alastair Hughes <hobbitalastair@gmail.com>
pkgname=ash-devel
pkgver=0.6
pkgrel=1
pkgdesc="Standard development utils"
arch=('any')
license=('MIT')
# Install needed packages
depends=(
        # missing base-devel packages
        'binutils' 'fakeroot' 'groff' 'libtool' 'texinfo'

        'fakechroot' 'parted' 'qemu-headless' 'qemu-headless-arch-extra'
        'git' 'perl-authen-sasl' 'perl-mime-tools' 'perl-net-smtp-ssl'
        'perf' 'strace' 'gdb'
        )

package() {
    :
}
