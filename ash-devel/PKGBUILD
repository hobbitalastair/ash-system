# Maintainer: Alastair Hughes <hobbitalastair@gmail.com>
pkgname=ash-devel
pkgver=0.4
pkgrel=1
pkgdesc="Standard development utils"
arch=('any')
license=('MIT')
# Install needed packages
depends=('markdown'
        'go'
        'ht'
        'gnuplot'
        'fakechroot' 'parted' 'qemu-headless' 'qemu-headless-arch-extra'
        'perl-authen-sasl' 'perl-mime-tools' 'perl-net-smtp-ssl' # For git...
        'perf' 'strace' 'gdb'
        )

package() {
    :
}
